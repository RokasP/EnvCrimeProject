@model SearchQueryDto

@{
    Layout = "_Staff";
}

<h2>Ärenden</h2>
<p class="info">Du är inloggad som avdelningschef</p>

<table id="managerForm">
    <tr>
        <td class="label">Välj status:</td>
        <td class="label">Välj handläggare:</td>
        <td>&nbsp;</td>
        <td class="label">Ärendenummer:</td>
        <td>&nbsp;</td>
    </tr>
    <tr>
        <form method="post" asp-controller="Manager" asp-action="StartManager">
            <td>
                <select asp-for="StatusId" asp-items="@(new SelectList(ViewBag.Statuses, "StatusId", "StatusName"))">
                    <option selected value="">Välj alla</option>
                </select>
            </td>
            <td>
                <select asp-for="EmployeeId" asp-items="@(new SelectList(ViewBag.Employees, "EmployeeId", "EmployeeName"))">
                    <option selected value="">Välj alla</option>
                </select>
            </td>
            <td><input class="button" type="submit" value="Hämta lista" /></td>
        </form>
        <form method="post" asp-controller="Manager" asp-action="StartManager">
            <td><input asp-for="RefNumber" /></td>
            <td><input class="button" type="submit" value="Sök" /></td>
        </form>
    </tr>
</table>

@await Component.InvokeAsync("ErrandList", Model)
