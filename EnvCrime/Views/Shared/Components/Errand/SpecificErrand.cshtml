@model Errand

<h2>Detaljer för ärendet</h2>
<p class="info">Du är inloggad som @ViewBag.RoleName</p>
<h3>Ärende: @Model.RefNumber</h3>

<section id="leftColumn">
    <h3>Anmälan</h3>
    <p>
        <span class="label">Typ av brott: </span><br />
        @Model.TypeOfCrime
    </p>
    <p>
        <span class="label">Brottsplats: </span><br />
        @Model.Place
    </p>
    <p>
        <span class="label">Brottsdatum: </span><br />
        @Model.DateOfObservation.ToShortDateString()
    </p>
    <p>
        <span class="label">Anmälare: </span><br />
        @Model.InformerName
    </p>
    <p>
        <span class="label">Telefon: </span><br />
        @Model.InformerPhone
    </p>
    <p>
        <span class="label">Observationer:</span><br />
        @Model.Observation
    </p>
</section>

<section id="rightColumn">
    <h3>Utredning</h3>
    <p>
        <span class="label">Status:</span><br />
        @ViewBag.StatusName
    </p>
    <p>
        <span class="label">Ansvarig avdelning: </span><br />
        @ViewBag.DepartmentName
    </p>
    <p>
        <span class="label">Handläggare: </span><br />
        @ViewBag.EmployeeName
    </p>
    <p>
        <span class="label">Ytterligare information: </span><br />
        @Model.InvestigatorInfo
    </p>
    <p>
        <span class="label">Händelser: </span><br />
        @Model.InvestigatorAction
    </p>
    <p>
        <span class="label">Provtagning: </span><br />
        @if (Model.Samples.Count > 0)
        {
            @foreach (var sample in Model.Samples)
            {
                <a href="../uploads/samples/@sample.SampleName">@sample.SampleName</a>
            }
        } else
        {
            <i>Inga dokument finns</i>
        }
    </p>
    <p>
        <span class="label">Bilder: </span><br />
        @if (Model.Pictures.Count > 0)
        {
            @foreach (var image in Model.Pictures)
            {
                <a href="../uploads/pictures/@image.PictureName">
                    <img src="../uploads/pictures/@image.PictureName" width="80" height="60" alt="klicka på bilden" />
                </a>
            }
        } else
        {
            <i>Inga bilder finns</i>
        }
    </p>
</section>
